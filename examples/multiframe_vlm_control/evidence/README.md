# Evidence (none)

This control exports no method memory evidence (no object crops, thumbnails,
relation visualizations, or retrieval traces) because it builds no object
memory. Raw sampled frames are tracked under `../raw_links/` as ablation input.
