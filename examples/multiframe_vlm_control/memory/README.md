# Memory (empty by design)

This no-explicit-memory control builds no object memory. There is no object
table, scene graph, caption store, or vector DB here.

`manifest.explicit_memory` is `false`; all Track 1-4 fixed APIs are `invalid`.
